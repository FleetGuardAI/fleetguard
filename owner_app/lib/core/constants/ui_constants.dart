class UIConstants {
  /// The global safe area height to prevent content from hiding behind the floating bottom navigation bar.
  /// 64 (nav height) + 16 (bottom padding) + 30 (extra margin) = 110.0
  static const double bottomNavSpace = 110.0;
}
